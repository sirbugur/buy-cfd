{
    "tasks": [
        {
            "type": "process",
            "label": "build buy",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "command": "make",
            "args": [
                "${fileDirname}/${fileBasenameNoExtension}",

            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": "$gcc",
            
        }
    ],
    "version": "2.0.0"
}